export const PADDLE_WIDTH = 2
export const BALL_RADIUS = 0.3
export const WIDTH = 20
export const HEIGHT = WIDTH * 3 / 4
export const EVENT_LOOP_PERIOD = 10

export const DEFAULT_BALL_POS = { x: 0, y: HEIGHT / 2 }
export const DEFAULT_BALL_ANGLE = Math.PI / 2
export const DEFAULT_BLOCKS = [
  { x: -WIDTH / 4, y: HEIGHT / 4 },
  { x: WIDTH / 4 - 1, y: HEIGHT / 4 }
]
