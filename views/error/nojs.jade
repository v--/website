doctype html
html
    head
        script.
            location.replace(typeof console === 'undefined' ? '/error/old' : '/');

        include ../_head
        base(href="/")
        link(rel='icon', href='/favicon.ico')
        link(rel='stylesheet', href='/styles/unsupported.css')
        title ivasilev.net &#187; Javascript is not enabled

    body
        h1 JavaScript is not enabled
        h2 Please enable javascript in order to view this website
