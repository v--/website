:marked
    # #{title}

    #{summary}. Inspired by [http://sorting-algorithms.com](sorting-algorithms.com).

    <b class="danger">__Legend:__</b> After clicking one of the ___'sort'___ buttons, the lines will begin to rearrange. Green means that the two lines were swapped, red means that they were not. You can run all algorithms at once to compare them.
