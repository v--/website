:marked
    # Sorting

    A visual comparison of array sorting algorithms. Inspired by [sorting-algorithms.com](http://sorting-algorithms.com).

b.danger Legend:

:marked
    The worst-case time and auxiliary space complexities are presented. After clicking one of the ___'sort'___ buttons, the lines will begin to rearrange. Green means that the two lines were swapped, red means that they were not. You can run all algorithms at once to compare them.

button.sorting-sortall(@click="sortall") Sort all

template(v-for="algorithm in algorithms")
    i-sorting-demo(:algorithm="algorithm")
