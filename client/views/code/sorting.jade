:marked
    #{summary} Inspired by the currently dead [sorting-algorithms.com](http://sorting-algorithms.com).

    <b class="danger">__Legend:__</b> The worst-case time and auxiliary space complexities are presented. After clicking one of the ___'sort'___ buttons, the lines will begin to rearrange. Green means that the two lines were swapped, red means that they were not. You can run all algorithms at once to compare them.
